# python-authenticity-service/Dockerfile
FROM python:3.11-slim

WORKDIR /app

# install your dependencies inline
RUN pip install --no-cache-dir \
      fastapi \
      uvicorn \
      requests \
      sqlalchemy \
      # …add any other libs you need

# copy your code
COPY . .

EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
